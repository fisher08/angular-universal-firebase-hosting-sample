/// <reference types="express-serve-static-core" />
import * as functions from 'firebase-functions';
import { ServerConfiguration } from 'angular-universal-express';
export declare type Trigger = functions.TriggerAnnotated & ((req: Express.Request, resp: Express.Response) => void);
export interface FirebaseConfiguration extends ServerConfiguration {
    staticDirectory?: string;
    cdnCacheExpiry: number;
    browserCacheExpiry: number;
    staleWhileRevalidate?: number;
}
/**
 * Create a Cloud Function HTTPS Trigger configured to generate
 * Angular Universal responses.
 * @param config
 */
export declare let trigger: (config: FirebaseConfiguration) => Trigger;
